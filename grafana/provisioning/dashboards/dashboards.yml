apiVersion: 1

providers:
  - name: 'PostgreSQL Dashboards'
    orgId: 1
    folder: 'PostgreSQL'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards  # Путь, куда мы монтируем JSON-файлы